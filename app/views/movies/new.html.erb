<%= form_for @movie do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title %>
    <%= f.label "check if already seen", :watched %>
    <%= f.check_box :watched %>
    <%= f.submit %>
<% end %>
    
